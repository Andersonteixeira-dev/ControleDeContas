<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ControleDeContas.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:ControleDeContas"
    xmlns:views="clr-namespace:ControleDeContas.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="ControleDeContas">

    <TabBar>
        <ShellContent
            Title="Contas"
            Icon="list_icon.png"
            ContentTemplate="{DataTemplate views:MainPage}"
            Route="MainPage" />

        <ShellContent
            Title="Relatórios"
            Icon="chart_icon.png"
            ContentTemplate="{DataTemplate views:GraficosPage}"
            Route="GraficosPage" />
    </TabBar>

</Shell>